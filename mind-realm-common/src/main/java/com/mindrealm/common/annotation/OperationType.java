package com.mindrealm.common.annotation;

/**
 * 操作类型枚举
 */
public enum OperationType {
    LOGIN,
    LOGOUT,
    INSERT,
    UPDATE,
    DELETE,
    SELECT,
    EXPORT,
    IMPORT,
    UPDATE_PASSWORD,
    RESET_PASSWORD,
    BIND_PHONE,
    BIND_EMAIL,
    SMS_SEND,
    WARNING_HANDLE,
    OTHER
}
